from django.urls import path
from django.conf.urls import url

from . import views

app_name = "payments"
urlpatterns = [
    # path("", views.ListCreatePaymentsView.as_view(), name="payments"),    
]