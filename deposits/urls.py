from django.urls import path
from django.conf.urls import url

from . import views

app_name = "deposits"
urlpatterns = [
    # path("", views.ListCreateDepositsView.as_view(), name="deposits"),    
]